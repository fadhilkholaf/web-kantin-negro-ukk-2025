const Footer = () => {
  return (
    <footer className="flex items-center justify-center p-2">
      <p>©️ {new Date().getFullYear()} Web Kantin Negro</p>
    </footer>
  );
};

export default Footer;
