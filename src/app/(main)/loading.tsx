const Loading = () => {
  return (
    <main className="flex h-screen w-full items-center justify-center bg-neutral/10 text-3xl">
      <h1 className="font-italiana text-primary">Loading</h1>
    </main>
  );
};

export default Loading;
